'use strict';

describe('myApp.levelOverview module', function() {

  beforeEach(module('myApp.levelOverview'));

  describe('levelOverview controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('levelOverviewCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});